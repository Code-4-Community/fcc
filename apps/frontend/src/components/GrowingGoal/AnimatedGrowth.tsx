import { ImpactProps } from '../ImpactCarousel/ImpactTile';

export type AnimatedGrowthProps = {
  currentProgress: number;
  goal: number;
};

/*
export const GrowingGoalProps = (props: ImpactProps) => {
  const { currentProgress, goal } = props;
     Return (
     …
     )
…
}*/
