export type ImpactProps = {
  title: string;
  description: string;
  image: string;
};

/*
export default function ImpactTile(props: ImpactProps) {
  const { title, image, description } = props;
  return (
    <.... Rendering of all of the props (one entire tile) …>
  );
}*/
